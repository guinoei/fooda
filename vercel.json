{
    "builds": [{
        "src": "fooda/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "fooda/wsgi.py"
        }
    ]
}